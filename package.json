{
  "name": "news-timer-app",
  "version": "1.0.0",
  "description": "A timer app to limit daily news reading time",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step needed for static files'",
    "vercel-build": "echo 'No build step needed'"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.2.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "vercel": "^48.0.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
